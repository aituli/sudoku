---
description: 项目编码规范 — 所有 TypeScript 文件
globs: "**/*.ts"
---

## TypeScript 规范
- 使用 strict 模式
- 禁止使用 `any` 类型，必须用具体类型或泛型
- 函数必须有明确的返回类型注解
- 每个函数体不超过 40 行
- 使用 `const` 优先，避免 `let`，禁止 `var`

## 命名规范
- 函数和变量：camelCase
- 类型和接口：PascalCase
- 常量：UPPER_SNAKE_CASE
- 文件名：kebab-case

## 模块规范
- 每个模块只做一件事
- core/ 下的模块禁止导入 ui/ 或访问 DOM
- 导出使用命名导出，避免 default export
