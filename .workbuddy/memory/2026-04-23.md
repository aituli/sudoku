# 2026-04-23

- 安装并启动了数独 H5 游戏项目（/Users/gd/Downloads/shudu），运行 `npm install` 和 `npm run dev`，服务在 localhost:18789 启动。
- 按 GitHub 标准重写了 README，生成英文版 `README.md`（默认）和中文版 `README.zh-CN.md`，包含功能特性、项目结构、架构说明、核心算法、部署方式等内容。
- 使用 Playwright 截取数独游戏运行截图（桌面端 + 移动端），保存至 `screenshots/` 目录，并在两个 README 中引用。
